apiVersion: v1
kind: Namespace
metadata:
  name: demo1-test
  labels:
    name: demo1-test
---
apiVersion: v1
kind: Pod
metadata:
  name: nginx-demo1
  namespace: demo1-test
  labels:
    app: nginx
    env: devtest1
spec:
  containers:
  - name: nginx
 #   image: nginx:1.14.2
    image: ssdeepu/demo:green
    resources:
      limits:
        memory: "512Mi"
        cpu: "500m"
      requests:
        memory: "256Mi"
        cpu: "250m"
    ports:
    - containerPort: 8080
---
    apiVersion: v1
    kind: Service
    metadata:
      name: demo1-service
      namespace: demo1-test
    spec:
      selector:
        app: nginx
      ports:
        - protocol: TCP
          port: 80
          targetPort: 8080
      type: LoadBalancer